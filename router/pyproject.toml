[project]
name = "aegis-router"
version = "0.1.0"
description = "AEGIS spoke-site router — on-prem DICOM receive, de-identify, and forward"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
