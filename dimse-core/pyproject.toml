[project]
name = "dimse-core"
version = "0.1.0"
description = "Shared DIMSE / SCP / retry / alert primitives used by AEGIS dimse-receiver and router"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["dimse_core*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
