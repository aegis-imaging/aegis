# AEGIS Defacing Service
#
# Python Cloud Run service for automated facial feature removal
# from head MRI/PET/CT scans.
#
# Key dependencies:
# - dcm2niix: DICOM to NIfTI conversion
# - mri_deface: facial feature removal (FreeSurfer)
# - pydicom + nibabel: pixel data injection back into DICOM

FROM python:3.12-slim

# TODO: Install dcm2niix (static binary)
# TODO: Install mri_deface + atlas files
# TODO: Install Python dependencies (pydicom, nibabel, fastapi, uvicorn)
# TODO: Add application code

WORKDIR /app
EXPOSE 8080

CMD ["echo", "defacing service placeholder"]
