# Union of phi-detection + qc-service + classification-service +
# protocol-service + synth-service + bids-service requirements.
fastapi>=0.104.0
uvicorn>=0.24.0
pydicom>=2.4.0
numpy>=1.24.0

# DICOM transfer-syntax decoders (used by every sub-module that touches pixels).
pylibjpeg>=2.0
pylibjpeg-libjpeg>=2.0
pylibjpeg-openjpeg>=2.0

# phi-detection: OCR over rendered pixel frames.
Pillow>=10.0.0
pytesseract>=0.3.10

# synth-service: phantom DICOM generation + multi-cloud upload of generated studies.
boto3>=1.34
azure-storage-blob>=12.19.0
azure-identity>=1.15.0
