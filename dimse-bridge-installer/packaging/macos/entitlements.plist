<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
        com.apple.security.network.server:
            Required to bind a listening socket. The bridge binds TCP 11112
            for DICOM C-STORE associations from local PACS/modalities.

        com.apple.security.cs.disable-library-validation:
            PyInstaller bundles its own Python interpreter + dynamic libs.
            Without this entitlement, hardened runtime refuses to load
            unsigned .dylibs from _internal/.
    -->
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
</dict>
</plist>
