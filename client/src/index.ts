/**
 * @aegis/client — DICOM anonymization and upload library
 *
 * Core modules (to be implemented):
 * - DICOM parser (dcmjs/dicomParser wrapper)
 * - De-identification engine (PS3.15 Annex E)
 * - Upload client (signed URL + chunked upload)
 */

export const VERSION = '0.1.0'
